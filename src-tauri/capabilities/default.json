{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window of M2M secure messenger",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
